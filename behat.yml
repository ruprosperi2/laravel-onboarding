default:
    suites:
        sale_order_features:
            paths:    [ '%paths.base%/features/sale_order' ]
            contexts: [ 'SaleOrderContext' ]

        request_order_features:
            paths: [ '%paths.base%/features/request_order' ]
            contexts: [ 'RequestOrderContext' ]
